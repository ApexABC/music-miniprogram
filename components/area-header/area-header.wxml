<!--components/area-header/area-header.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view class="more" bindtap="handlerMore" wx:if="{{hasMore}}">
    <text>更多</text>
    <van-icon name="arrow" />
  </view>
</view>